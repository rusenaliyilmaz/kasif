[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kasif"
version = "0.1.0"
description = "Dependency discovery and source lookup CLI for Defter"
requires-python = ">=3.9"

[project.scripts]
kasif = "kasif.cli:main"
